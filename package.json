{
  "name": "code-review-checklist-agent",
  "displayName": "Code Review Checklist Agent",
  "description": "PSSM2.0 Code Review Checklist Agent — conversational AI that generates, analyses and tracks code review checklists from GitHub PRs and Jira stories",
  "version": "2.2.1",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "AI",
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "chatParticipants": [
      {
        "id": "decisiv-pssm.code-review",
        "fullName": "Code Review Checklist",
        "name": "code-review",
        "description": "Generates a filled PSSM2.0 code review checklist Excel from PR comments",
        "isSticky": true,
        "commands": [
          {
            "name": "generate",
            "description": "Generate a filled checklist Excel for a PR. Usage: @code-review /generate <PR_URL>"
          },
          {
            "name": "token",
            "description": "Save, check, or clear your GitHub PAT. Usage: @code-review /token <PAT> | status | clear"
          },
          {
            "name": "help",
            "description": "Show usage instructions"
          },
          {
            "name": "history",
            "description": "Show all checklist versions for a PR. Usage: @code-review /history <PR_URL>"
          },
          {
            "name": "jira",
            "description": "Save Jira credentials. Usage: @code-review /jira <email> <token> | status | clear"
          },
          {
            "name": "list",
            "description": "Batch mode: resolve Jira keys/PR URLs and generate checklists. Usage: @code-review /generate --list SRM2-1234 SRM2-5678 https://github.com/org/repo/pull/42"
          },
          {
            "name": "fix-review",
            "description": "Walk through every unresolved PR comment + Not Ok checklist row with AI-powered fix suggestions and PR reply comments"
          }
        ]
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run bundle",
    "bundle": "npx esbuild src/extension.ts --bundle --outfile=out/extension.js --external:vscode --format=cjs --platform=node --minify",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "jest --config jest.config.json",
    "test:coverage": "jest --config jest.config.json --coverage"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "jszip": "^3.10.1",
    "xlsx": "^0.18.5",
    "xlsx-populate": "^1.21.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.90.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.28.2",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.12",
    "typescript": "4.9.5"
  },
  "publisher": "decisiv-pssm",
  "repository": {
    "type": "git",
    "url": "https://github.com/Decisiv/pssm"
  }
}